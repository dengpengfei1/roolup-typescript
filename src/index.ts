'use strict'

import { aaa } from './interface/index'
let a:aaa = {
    b: '2',
    a: 3
}

let arr = Array.from(new Set(['a', 'b', 'c']))
export let obj = Object.assign({}, {a: 1})
export default arr
